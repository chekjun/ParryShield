if (!instance_exists(obj_enemy_single)
		and !instance_exists(obj_enemy_burst)
		and !instance_exists(obj_enemy_shotgun)
		and !instance_exists(obj_enemy_fan)
		and !instance_exists(obj_narima)
		) {
	cleared = true;
} else {
	cleared = false;
}